services:
  database:
    image: mariadb:latest
    container_name: watchdog-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: watchdog_db
    ports:
      - "3306:3306"
#    volumes:
#      - ./mysql_data:/var/lib/mysql

#  app:
#    build: .
#    container_name: watchdog-app
#    volumes:
#      - /var/run/docker.sock:/var/run/docker.sock
#    depends_on:
#      - database

# implemend above when dockerfile is correctly integrated